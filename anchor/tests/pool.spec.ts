import { SetupDTO } from "./common-setup";


describe("Pool", () => {
    let setupDto: SetupDTO;

    beforeAll(async () => {
        setupDto = await commonSetup();
    });

    it("Create pool counter successfully", async () => {});

    it("Update pool counter successfully", async () => {});

});